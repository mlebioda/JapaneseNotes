## 07:34 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: improvement
**What changed**: Vault system diagram regenerated to reflect current agent and skill roster — orchestrator moved from the active Agents package to the Deprecated package, skill-updater confirmed deprecated, all 7 agents and 15 skills captured with accurate A2A edges, storage reads/writes, and skill chains.
**Why**: The previous version had orchestrator incorrectly placed in the active Agents package despite it being deprecated; the Deprecated block was incomplete and A2A edge annotations across agents had drifted from the actual agent file contents.
**Lesson**: **[Rule]** When a diagram has both an active and a deprecated package, always verify the package placement of every component against the current agent/skill status — a deprecated agent listed in the active package is more misleading than a missing node, because it actively directs design reviews toward a component that should no longer be used.
**Commit**: uncommitted

---

## <current> — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: improvement
**What changed**: Diagram refined with accurate storage node granularity (Caligraphy/ split into Kanji/ and Primitives/, JPLessons/Reading/ as separate node, ICSRoot renamed from VaultRoot), two lesson-to-web reference components added (_conventions and _patterns), .cowork/tmp/ removed (deprecated-agent artefact), Planner→Reviewer A2A edge added, and note formatting standardised throughout all agents and skills.
**Why**: Previous diagram used coarse storage nodes (single Caligraphy/ database, single Lessons/ node for Reading output) that masked real access patterns; lesson-to-web reference files were read unconditionally by skills but not modelled as nodes; .cowork/tmp/ was retained from the deprecated orchestrator; and note text had formatting drift across components.
**Lesson**: **[Rule]** When modelling storage access in a documentation diagram, split every directory that has meaningfully different read/write access patterns into separate database nodes — a single coarse node for a directory with distinct sub-directories hides which skill or agent touches which subtree, making access-rule audits unreliable.
**Commit**: uncommitted

---

## 14:29 — Plans/practice-grammar-study-mode-v2-plan.md

**Called by**: planner
**Classification**: new-feature
**What changed**: Plan created to replace the between-batch "Ready for next batch?" prompt with deliberate "Next batch / Study mode" and "Study mode / Save & finish" named-choice prompts, and to add a Study Mode interlude with topic selection, explanation referencing user answers, a 6-exercise practice batch covering all exercise types, and a follow-up question loop.
**Why**: The existing between-batch flow offered only a passive "ready?" confirmation, giving the user no structured way to pause and study a grammar point in context before continuing drills; the new plan makes Study Mode a first-class, named option at every batch boundary.
**Lesson**: **[Rule]** When a drill skill presents a between-round prompt, replace open-ended "ready?" confirmations with a named-choice menu that surfaces every available mode (drill, study, finish) — named choices make the study path discoverable and prevent users from continuing blindly when they need to review.
**Commit**: uncommitted

---

## 08:08 — Plans/practice-grammar-scope-triggers-plan.md

**Called by**: planner
**Classification**: new-feature
**What changed**: Plan created to extend practice-grammar with two mutually exclusive scope-based triggers — "today's topics" (next_review == today) and "overdue topics" (next_review < today) — drawn from the existing SM-2 state file.
**Why**: Users need a way to start a drill session scoped to what is due today or what is overdue, without manually naming a lesson file.
**Lesson**: **[Rule]** When adding multiple scope-based trigger variants to a drill skill, define each scope as a mutually exclusive filter with an explicit date comparison operator (==, <, <=) and document which SM-2 field drives the filter — ambiguous or overlapping scopes produce unpredictable session content.
**Commit**: uncommitted

---
