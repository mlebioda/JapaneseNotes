## 09:48 — wc-honorific-special-verb-plan.md

**Called by**: planner
**Classification**: new-feature
**What changed**: New plan created to rename the `#wc` verb template field `お〜になる (honorific)` to `お〜になる/special verb (honorific)` and replace its single-mode derivation rule with a lesson-derived three-tier honorific logic (special-verb table → お+ます形+になる → web-search-confirmed dash), including a correction to 来る's fixed value and a new templates-update.md repair step (Repair 3d) for verification.
**Why**: The existing derivation rule assumed お+ます形+になる applied unconditionally, which produced an incorrect fixed value for 来る (お出でになる instead of いらっしゃる) and did not account for verbs with dedicated special-verb forms (食べる→召し上がる, etc.) that should always outrank the general derivation pattern.
**Lesson**: **[Rule]** When a template field derives its value from a single general formation rule, check the domain for known irregular/special-case lexical items first — encode them as a higher-priority tier (special-form table) that always wins over the general rule, and add a verification/repair step so already-filled files can be recomputed against the corrected logic.
**Commit**: `uncommitted` — new plan file `Plans/wc-honorific-special-verb-plan.md` (untracked)

---

## 14:10 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Diagram regenerated to add `.cowork/scripts/` and `.claude/scripts/` as distinct storage nodes, rename several aliases for clarity (Lessons→LessonsDir, GrammarIndex→GrammarIndexDir, VocabDir→VocabularyDir), restore per-skill detail (kanji-file's external WebFetch note, git-commit's read/write edges, practice-grammar-study-mode's early-exit chain to the SRS sub-skill), and add two explicit GAP notes: skill-implementer's note now states it does not call scribe automatically via a `subagent_type: scribe` block — contradicting instructions.md's claim that "skill-implementer calls scribe automatically" — and git-commit's note flags that its git push behaviour is inconsistent with the "never push to git" hard rule that binds every agent.
**Why**: Routine documentation regeneration after recent skill/agent changes; the regeneration process itself functions as a consistency audit between instructions.md's claims and the actual file contents of agents/skills.
**Lesson**: **[Rule]** When regenerating a system diagram, explicitly cross-check every behavioural claim made in instructions.md (or CLAUDE.md) against the actual content of the referenced agent/skill file, and encode any contradiction as a visible GAP note in the diagram — a diagram that only draws what exists misses the more valuable finding that what is documented as a guarantee is not actually implemented.
**Commit**: uncommitted

---

## 15:12 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Diagram regenerated again within the same session: added `.cowork/scripts/` and `.claude/scripts/` as distinct storage nodes, renamed several aliases for clarity (Lessons→LessonsDir, GrammarIndex→GrammarIndexDir, VocabDir→VocabularyDir, VaultRoot→KanjiListFile/VaultRootIcs), and restored per-skill detail (kanji-file's WebFetch note, git-commit's read/write edges, practice-grammar-study-mode's SRS chain note). In the process, the two GAP notes added by the prior 14:10 regeneration in this same session — the skill-implementer "does not call scribe automatically" contradiction with instructions.md, and git-commit's push behaviour conflicting with the vault-wide "never push to git" rule — were silently dropped from the new version.
**Why**: Routine documentation regeneration to reflect current agent/skill file contents; however, since the diagram is regenerated from source files each time rather than diffed against its own immediately-prior version, GAP notes added moments earlier in the same session were lost when this pass rewrote the same sections.
**Lesson**: **[Rule]** A documentation/diagram regeneration must diff against its own last-saved version (not just re-derive from source files) before overwriting — findings such as GAP/contradiction notes that were added in a previous pass are not re-derivable from source alone and will be silently lost if the new pass fully replaces the file.
**Commit**: uncommitted

---
