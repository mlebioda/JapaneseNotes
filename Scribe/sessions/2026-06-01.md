# Scribe Session — 2026-06-01

## 00:00 — _patterns.md (created)

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: Created `.cowork/skills/lesson-to-web/_patterns.md` — a lazy-loaded input recognition registry for the lesson-to-web pipeline. Contains a lazy-load rule and a seed entry for the trailing furigana format (fullwidth or ASCII parens after a word group), moving this documented pattern out of preprocess-grammar and into a shared, extensible registry.
**Why**: The trailing furigana pattern was previously described inline in preprocess-grammar Step 3. Moving it to a shared registry decouples input-format decisions from step logic and allows any skill in the pipeline to consult the same decision record without duplicating documentation.
**Lesson**: **[Pattern]** When a skill embeds documentation about a specific input format variant, that documentation is a candidate for a shared input recognition registry — particularly when other skills in the same pipeline may encounter the same format.
**Commit**: uncommitted

---
## 00:00 — preprocess-grammar.md (Task 5)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Removed the `## Never touch` section from `preprocess-grammar.md`. Rules now live exclusively in the shared `_conventions.md` file.
**Why**: The Never touch rules were duplicated across all six lesson-to-web skill files. Centralising them in `_conventions.md` ensures a single source of truth and prevents drift when rules change.
**Lesson**: **[Rule]** When the same constraint list (e.g. "Never touch") appears verbatim in multiple skill files, move it to a shared conventions file and replace each copy with a reference — this eliminates silent divergence when constraints evolve.
**Commit**: uncommitted

---
## 00:00 — structure-grammar.md (Task 10)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Added `## Shared files` section at the top of the `structure-grammar.md` workflow, before Step 7. Instructs the skill to load `_conventions.md` before starting.
**Why**: Part of the lesson-to-web refactor — every skill now declares its shared-file dependencies explicitly at the top of the workflow rather than relying on implicit convention.
**Lesson**: **[Pattern]** Placing a "Shared files" declaration at the top of each skill workflow makes load-order dependencies explicit and machine-readable — the same benefit that `import` statements provide in code.
**Commit**: uncommitted

---
## 00:00 — structure-grammar.md (Task 12)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Removed the `## Never touch` section from `structure-grammar.md`. Rules now live exclusively in the shared `_conventions.md` file.
**Why**: Part of the lesson-to-web refactor — Never touch rules are duplicated across all six skill files. Centralising them in `_conventions.md` ensures a single source of truth.
**Lesson**: **[Rule]** When the same constraint list appears verbatim in multiple skill files, consolidate to a shared conventions file and reference it — prevents silent constraint divergence as the system evolves.
**Commit**: uncommitted

---
## 00:00 — extract-grammar.md (Task 18)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Removed the `## Never touch` section from `extract-grammar.md`. Rules now live exclusively in the shared `_conventions.md` file.
**Why**: Part of the lesson-to-web refactor — Never touch rules were duplicated across all six lesson-to-web skill files. Centralising them in `_conventions.md` ensures a single source of truth and prevents drift when constraints evolve.
**Lesson**: **[Rule]** Removing duplicated constraint blocks from individual skill files in favour of a shared conventions file reduces the maintenance surface — any future constraint change needs to happen in exactly one place.
**Commit**: uncommitted

---
## 00:00 — extract-grammar.md (Task 19)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Removed the `## File placement` section from `extract-grammar.md`. The file-path constants for grammar-index output directories have moved to the shared `_conventions.md` file.
**Why**: File placement paths were duplicated between `extract-grammar.md` and the new `_conventions.md`. Removing the local copy makes `_conventions.md` the single source of truth for output locations and prevents divergence if paths change.
**Lesson**: **[Rule]** When a skill file contains absolute-path constants that also appear in a shared conventions file, remove the local copy — having two places to update a path is a maintenance hazard.
**Commit**: uncommitted

---
## 00:00 — update-grammar.md (Task 20)

**Called by**: orchestrator
**Classification**: improvement
**What changed**: Added a one-line load instruction at the top of the `## Workflow` section in `update-grammar.md`: "Load `.cowork/skills/lesson-to-web/_conventions.md` before starting."
**Why**: update-grammar is the entry point for the full pipeline chain. It must declare the shared conventions dependency explicitly so that any agent loading only this skill knows to also load _conventions.md before delegating to preprocess-grammar.
**Lesson**: **[Rule]** A pipeline-entry skill that delegates to sub-skills must still declare shared-file dependencies explicitly — relying on the sub-skills to load them silently makes the entry point's contract incomplete.
**Commit**: uncommitted

---

## 00:00 — orchestrator.md

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Added an explicit Bash write prohibition to orchestrator's hard rules, closing a loophole that allowed the orchestrator to bypass skill-implementer and write vault files directly via Bash tool calls.
**Why**: The orchestrator's existing write-prohibition rules targeted Read/Write/Edit tool calls but did not name Bash, so Bash remained a viable (and used) path for direct vault writes that should only flow through skill-implementer.
**Lesson**: **[Rule]** When prohibiting direct file writes in an orchestrator, name every write-capable tool explicitly — Bash, Write, Edit, and Read-with-write patterns — because a prohibition that omits one tool name leaves an exploitable loophole.
**Commit**: uncommitted

---
