## 08:02 — templates-update.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Repair numbering corrected (6→5, 7→6), new Repair 4b added for adjective form re-verification, Repair 2 skip condition updated, Repair 1 silent-strip clarification added, Repair 5 (formerly 6) now delegates kanji link generation to kanji-links.py via Bash, duplicate "## Never touch" section merged into canonical "## What never to touch", and verb-type heuristic block replaced with a pointer to references/verb-conjugation.md.
**Why**: Reviewer identified a numbering gap (Repair 5 was absent), a missing adjective form verification step mirroring the existing verb form verification, and structural duplication between two near-identical "never touch" sections. Planner incorporated two user decisions and five editorial fixes.
**Lesson**: **[Rule]** When a skill is authored incrementally, audit repair/step numbers explicitly before publishing — gaps (e.g. Repair 5 missing while Repair 6 exists) are invisible to the reader and cause confusion when later repairs are added.
**Commit**: uncommitted

---

## 08:02 — references/kanji-links.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: "Script usage" section added describing how to call kanji-links.py, specifying that the caller must strip furigana before passing source text.
**Why**: Repair 5 in templates-update.md now delegates kanji-link generation to kanji-links.py; the reference file needed to document the calling convention so skill authors know both the interface and the furigana pre-processing responsibility.
**Lesson**: **[Rule]** When a skill delegates a mechanical task to an external script, the reference file for that task domain must document the script's calling convention and any pre-processing the caller is responsible for — do not leave the interface implied.
**Commit**: uncommitted

---

## 08:02 — kanji-links.py (new file)

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: New stdlib-only Python script added at .cowork/scripts/kanji-links.py; accepts pre-stripped source text as a single argument, scans for CJK characters (U+4E00–U+9FFF), deduplicates in first-occurrence order, and prints one `<a href>` line per kanji to stdout.
**Why**: Repair 5 of templates-update.md required reliable kanji link generation. Delegating Unicode scanning to a small, deterministic script is more reliable than having the LLM perform inline character-range detection, and frees LLM context for reasoning-heavy repairs.
**Lesson**: **[Rule]** Delegate mechanical text-processing tasks (Unicode scanning, regex matching, deduplication) to a small stdlib script rather than performing them inline in a skill — the script is deterministic, testable, and frees LLM context for work that requires judgment.
**Commit**: uncommitted

---

## 08:02 — instructions.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: templates-update skill added to the Available skills list with all four trigger phrases and a one-sentence description of what it does.
**Why**: The skill was renamed from update-templates.md to templates-update.md in the same session; the instructions.md entry was added to keep the discovery index accurate and expose the correct trigger phrases.
**Lesson**: **[Rule]** Every time a skill is renamed or created, update instructions.md's Available skills list in the same change set — stale or missing entries cause the skill to be invisible to both human users and the orchestrator.
**Commit**: uncommitted

---

## 10:00 — templates-update.md

**Called by**: reviewer
**Classification**: missing-rule
**What changed**: Reviewer identified two omissions in the current file: (1) Step 4's instruction "Apply changes using targeted Edit calls" provides no bulk-repair path for large files — when 130+ cards need kanji-link repairs, the LLM silently deviates because the permitted mechanism doesn't scale; (2) the skill has no pre-execution git-commit guardrail despite being a destructive skill that can overwrite conjugation and adjective form values across an entire file.
**Why**: Step 4 has no bulk-repair path — "Apply changes using targeted Edit calls" does not permit or describe wrapper scripts for large files, causing silent deviation when 130+ cards need kanji-link repairs; additionally there is no pre-execution git-commit guardrail for this high-risk destructive skill.
**Lesson**: **[Rule]** Destructive skills that rewrite values across many cards must (a) include a git-commit guardrail at the top of the workflow and (b) explicitly permit bulk-repair mechanisms when the affected block count exceeds a threshold — "use targeted Edits" alone is insufficient guidance for large files.
**Commit**: uncommitted

---

## 11:11 — templates-update.md

**Called by**: reviewer
**Classification**: design-oversight
**What changed**: Three structural gaps identified in templates-update.md — (1) skill reads the full lesson file instead of only the post-Summary section, wasting context; (2) no WIP git guardrail before destructive edits; (3) the block-parsing step does not resolve ambiguity introduced by the legacy Rzeczowniki: label.
**Why**: Review completed. Findings: missing read optimisation (reads full file instead of post-Summary section only), absent WIP git guardrail before destructive edits, and block-parsing ambiguity around legacy Rzeczowniki: label.
**Lesson**: **[Rule]** When a skill operates only on a known sub-section of a file (e.g. post-Summary), its read instruction must scope to that section explicitly — reading the full file wastes context budget and obscures which content is in scope.
**Commit**: uncommitted

---
