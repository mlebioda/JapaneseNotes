# Scribe Session — 2026-05-31

## 00:00 — orchestrator.md

**Called by**: planner
**Classification**: new-feature
**What changed**: Plan created for fixing eight structural bugs in orchestrator.md — A2A wiring, revision loop, slug resolution, post pipeline, DONE block accuracy, and explicit write-prohibition rules.
**Why**: Reviewer identified that the orchestrator had a synthetic TARGET for `new` tasks, a broken revision loop using an undefined `REVISE: true` field, no mechanism to resolve the slug planner chose, no pipeline for `post` task types, inaccurate DONE block scribe attribution, and no explicit prohibition rules preventing the orchestrator from writing files directly.
**Lesson**: **[Rule]** When designing an orchestrator agent, explicitly prohibit write access in its hard rules and define a concrete mechanism for passing state (e.g. slug, reviewer output) between stages rather than relying on implicit context.
**Commit**: uncommitted

---

## 12:00 — orchestrator.md

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Applied all 9 A2A wiring fixes to orchestrator.md: synthetic TARGET replaced with real Bash lookup, broken REVISE loop corrected to use REVIEWER_BRIEF+REVISION_NOTES, slug resolution step added between Stage 2 and 3, post task type removed as out-of-scope, DONE block Scribe attribution corrected, Hard rules expanded to prohibit direct Edit/Write and stage-skipping, temp file exception added, skill-updater DEPRECATED added to roster.
**Why**: Orchestrator had structural bugs where new-task TARGET was a synthetic string (not a real path), the revision loop used an undefined REVISE field, the implementer could be called without knowing which plan file to load, and there were no explicit write-prohibition rules preventing the orchestrator from acting as an implementer.
**Lesson**: **[Rule]** Never design an orchestrator that passes synthetic or inferred path strings between stages — always resolve real filesystem paths via Bash before delegating to a sub-agent, and document the resolution mechanism explicitly in the agent definition.
**Commit**: uncommitted

---

## 16:09 — orchestrator.md (second pass)

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Second-pass A2A wiring fixes: added no-issues branch at Stage 1 reviewer gate, replaced ls -t slug resolution with temp-file handoff at .cowork/tmp/orchestrator-handoff.md, standardised all call templates to relative paths, added CONSTRAINT field to Stage 4 reviewer call to prevent it from routing around the orchestrator, tightened Hard rules to remove the ambiguous review-only scribe exception.
**Why**: First-pass orchestrator wiring left the no-issues reviewer outcome unhandled (silent failure), used an mtime-based slug resolution that could break on iCloud vaults, used absolute paths in call templates that would break if vault is moved, allowed Stage 4 reviewer to ask the user for further routing (bypassing the orchestrator gate), and had an ambiguous Hard rule permitting scribe calls in a "review-only path" that was never clearly defined.
**Lesson**: **[Rule]** In a multi-stage agent pipeline, handle every possible sub-agent outcome explicitly (including the no-issues branch), use a dedicated temp-file handoff for inter-stage state rather than filesystem mtime, and add a CONSTRAINT field to sub-agent calls to prevent them from issuing routing instructions that bypass the orchestrator.
**Commit**: uncommitted

---

## 16:09 — planner.md

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Documented `REVISION_NOTES` as an optional A2A input field in planner's expected-input format block, with an instruction to incorporate it directly rather than re-asking the user.
**Why**: The orchestrator's Stage 2 revision branch passes `REVISION_NOTES` to the planner, but the planner's input schema did not list it, so the planner had no documented instruction for how to handle user feedback on a draft plan.
**Lesson**: **[Rule]** Always declare every optional A2A input field in an agent's expected-input format block and provide explicit handling instructions, so callers can pass structured data without the agent re-asking the user for information it has already received.
**Commit**: uncommitted

---

## 16:10 — orchestrator.md, reviewer.md, planner.md, skill-implementer.md, scribe.md

**Called by**: skill-implementer
**Classification**: missing-rule
**What changed**: Added a standardised error-handling protocol to all five agent files (stop-and-report on file-not-found, A2A failure, user-cancel, and unexpected output), split Scribe/lessons-learned.md into three audience-specific files (agent / skill / general), and added a commit-message suggestion step to skill-implementer.
**Why**: No agent had documented behaviour for failure conditions — silent continuation past a failed sub-agent call left the pipeline in an incomplete and misleading state. The single lessons-learned.md file was also too coarse-grained for authors looking for domain-specific guidance.
**Lesson**: **[Rule]** Every agent definition must include an explicit Error handling section that covers file-not-found, A2A call failure, and unexpected output — and must specify "stop and report" as the default response rather than silent continuation.
**Commit**: uncommitted

---
