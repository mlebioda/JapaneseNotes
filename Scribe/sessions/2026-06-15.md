## 12:00 — practice-grammar.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Replaced four vague exercise types with six JLPT-aligned types, added deterministic type-selection and weak-point bias rules, added hidden-target rule (grammar point name suppressed until after submission), tightened Gate 1 wording, updated batch-mode layout to suppress grammar-point name from headers, and added grading rules for Types 5 and 6.
**Why**: The original exercise types (Translate to Japanese, Fill the blank, Choose the correct form, Build from pieces) were too loosely defined, not aligned with JLPT question formats, and had no principled selection or difficulty bias logic.
**Lesson**: **[Rule]** When designing a drill skill, define exercise types as a closed, named set aligned with the target exam format, and specify a deterministic selection algorithm — open-ended exercise variety produces inconsistent sessions and makes grading rules impossible to specify precisely.
**Commit**: uncommitted

---

## 12:30 — practice-grammar.md

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: Extended practice-grammar with scope-based triggers (due_today / overdue / most_overdue N), lazy lesson-file loading with per-lesson caching, pre-session batch split negotiation, a batch loop with a mode-choice prompt after each batch, a session-wide score buffer written only at the end, and an optional Study Mode interlude for deep-dive explanation without scoring.
**Why**: The existing skill only supported single-lesson drills; users needed cross-lesson spaced-repetition review that respects the grammar-state.json due dates and allows breaking long review queues into manageable batches with a study escape hatch between them.
**Lesson**: **[Rule]** When a drill skill grows to support cross-lesson scope queries, buffer all self-evaluation scores in a session scratch object and write grammar-state.json once at the very end — mid-session writes create partial state that is inconsistent if the session is interrupted.
**Commit**: uncommitted

---

## 07:23 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Vault system diagram fully regenerated — all 7 agents and 18 skills (including lesson-to-web subskills, references, and deprecated entries) re-extracted, with A2A wiring, skill chains, and storage access rules.
**Why**: Diagram overwritten to reflect the current complete state of the system after accumulated additions and structural changes.
**Lesson**: **[Rule]** When regenerating a documentation diagram from scratch, always enumerate every component category explicitly (agents, skills, subskills, deprecated entries, storage nodes, A2A edges) — a diagram regenerated by diff-patching misses categories that were never touched but have drifted from reality.
**Commit**: uncommitted

---

## 09:40 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Diagram refined after full re-extraction from all 7 agent files and 19 skill files — added vault-root *.ics storage node, practice-grammar scope-based mode notation, reading-jlpt session-file write, update-kanji-list deprecated skill package, and tightened agent notes across orchestrator, reviewer, planner, skill-implementer, scribe, and documentation agents.
**Why**: Previous regeneration captured 18 skills and omitted several storage nodes and agent-note refinements; a second full pass over all source files closed those gaps.
**Lesson**: **[Rule]** After regenerating a documentation diagram, always cross-check the component count against the actual skill/agent directory listing — a count discrepancy (e.g. 18 vs 19 skills) signals that at least one component was silently omitted.
**Commit**: uncommitted

---

## 10:32 — kanji-file.md

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Step 4 now removes broken bare wikilinks (no file found) and reports them as REMOVED entries; Step 5 classifies surviving bare links by destination (component links without `#` → `### Parts`, lesson links with `#` → `## Occurences`); Step 6 added a guard that warns on any bare link still outside a named `##` section after Steps 4–5; completion report extended with REMOVED count and `### Parts` migration count.
**Why**: Step 4 previously only warned about broken bare links instead of removing them; Step 5 sent all bare links to `## Occurences` regardless of type, silently misplacing structural component links that belong in `### Parts`; Step 6 had no check for lingering unsectioned bare links; the completion report format lacked REMOVED and Parts-migration counts.
**Lesson**: **[Rule]** When a skill migrates stale content into sections, it must classify each item's destination before migrating — a single-destination migration step silently misplaces items that belong to different sections, and broken items must be removed rather than preserved with a warning.
**Commit**: uncommitted

---

## 11:45 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: improvement
**What changed**: Diagram notes refined to reflect implementation details accumulated since the last regeneration — practice-grammar notes updated with scope-based triggers, Study Mode batch interlude, configurable batch split, and single end-of-session JSON write; kanji-file notes updated with decimal Unicode code point URL format and mnemonic-non-empty skip guard; templates-update notes updated with named repair step identifiers (Repair 3b, 4b); update-kanji-list deprecated note extended with "do not invoke" warning; minor wording improvements across extract-grammar, preprocess-grammar, review-grammar, structure-grammar, and see-also-grammar.
**Why**: After each implementation pass the diagram notes lagged behind the actual skill behaviour; specific mechanic names (named repair steps, URL encoding scheme, buffering strategy) were absent, making the diagram less useful as a reference.
**Lesson**: **[Rule]** After each skill improvement pass, update the diagram note for that skill with the specific mechanic names and behavioural contracts added — a diagram note that omits implementation details (named steps, encoding schemes, write-timing rules) is less useful than the skill file itself and will drift from reality.
**Commit**: uncommitted

---

## 11:13 — Plans/sequential-kanji-fetch-plan.md

**Called by**: planner
**Classification**: new-feature
**What changed**: New plan created to add sequential-fetch wording to kanji-file (Step B) and kanji-headers (Step 2) to prevent HTTP 429 rate-limit errors from kanji-trainer.org when multiple kanji are processed in a single run.
**Why**: kanji-trainer.org returns HTTP 429 when fetch requests are issued concurrently; the previous skill wording contained no explicit instruction to serialize requests, leaving the rate-limit behaviour implicit and unenforced.
**Lesson**: **[Rule]** When a skill fetches from an external site that enforces rate limits, add an explicit sequential-processing rule at the exact fetch step — never leave the serialization requirement implicit, as concurrent requests will be issued by default when multiple items are processed in a single run.
**Commit**: uncommitted

---

## 13:16 — kanji-headers.md

**Called by**: skill-implementer
**Classification**: missing-rule
**What changed**: Added a Sequential processing rule to Step 2 requiring kanji-file to be called one at a time and waited on before the next, prohibiting parallel calls to prevent HTTP 429 rate-limit errors from kanji-trainer.org.
**Why**: The skill had no explicit instruction to serialize kanji-file calls; without it parallel invocations were the default, causing rate-limit failures when a lesson header contained multiple kanji.
**Lesson**: **[Rule]** When a skill calls a sub-skill that fetches from an external site, add an explicit "call one at a time, wait for completion before the next" rule at the exact invocation step — parallelism is the implicit default and will trigger rate-limit errors without this guard.
**Commit**: uncommitted

---

## 13:16 — kanji-file.md

**Called by**: skill-implementer
**Classification**: missing-rule
**What changed**: Added an explicit Rate-limit rule in Step B: fetches to kanji-trainer.org must be done sequentially, one at a time; parallel or concurrent requests are prohibited even when kanji-file is called for multiple kanji in a single run.
**Why**: The skill had no instruction to serialize requests to kanji-trainer.org; without this rule an LLM executing the skill for multiple kanji would issue concurrent fetches by default, triggering HTTP 429 rate-limit errors from the site.
**Lesson**: **[Rule]** When a skill fetches from an external site that enforces rate limits, add an explicit sequential-processing rule at the exact fetch step — never leave the serialization requirement implicit, as concurrent requests will be issued by default when multiple items are processed in a single run.
**Commit**: uncommitted

---

## 14:00 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: improvement
**What changed**: Diagram wording sanitised — Unicode em-dashes in comment arrows replaced with ASCII `->`, Japanese passage-type labels (短文/中文/情報検索) replaced with English equivalents, "sequential" qualifier added to kanji-headers chain annotation, and `i/na/non-adj` normalised in adj-forms note.
**Why**: After each regeneration pass, residual Unicode characters and non-ASCII labels (Japanese text in note blocks) were found in the diagram source; these survive PlantUML rendering but break diffs, grep, and tool-based reads that expect plain ASCII.
**Lesson**: **[Rule]** In a documentation diagram source file, use only ASCII characters in comment lines, arrow labels, and note blocks — Unicode arrows and non-ASCII labels survive rendering but silently break diff readability, grep, and any tool that expects plain-text content.
**Commit**: uncommitted

---

## 10:26 — Plans/kanji-file-bare-link-fix-plan.md

**Called by**: planner
**Classification**: bug-fix
**What changed**: New plan created to fix two bugs in kanji-file: Step 4 must remove broken bare wikilinks (no file found) instead of issuing a warning, and Step 5 must route surviving bare component links to ### Parts rather than ## Occurences.
**Why**: Running kanji-file on existing kanji files with old-style bare wikilinks sent all bare links to ## Occurences — structural component links (e.g. [[十-ten,10]] as a part of 千) ended up in the wrong section, and broken links (e.g. [[丿 - component]]) were kept rather than cleaned up.
**Lesson**: **[Rule]** When a skill migrates stale content into sections, it must classify each item's destination before migrating — a single-destination migration step silently misplaces items that belong to different sections, and broken items must be removed rather than preserved with a warning.
**Commit**: uncommitted

---
