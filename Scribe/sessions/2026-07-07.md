## 08:34 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: design-oversight (caller supplied `new-feature`; overridden — see note below)
**What changed**: Diagram was fully regenerated and shrank from 607 to 305 lines. Several storage-node splits established in prior iterations (Caligraphy/Kanji, Caligraphy/Primitives, JPLessons/Reading, a dedicated ICS-export root) were collapsed back into coarser single nodes, and most per-skill notes (trigger phrases, script paths, chain annotations, granular read/write edges) were condensed to one-line summaries. New content was also added: a `.cowork/skills/references/` node, an explicit "orphaned skill" flag on `git-commit` (not listed in instructions.md's Available Skills registry), and a flag on `fill-templates` about a hardcoded remote mount path inconsistent with the vault-root convention.
**Why**: Caller reported "vault system diagram generated/regenerated" with no further detail. Comparing the new content against the previously committed version (and against the diagram's own change history in `agent-lessons-learned.md`) shows this regeneration reversed several deliberate granularity improvements made in the 2026-06-15/06-16 passes (storage-node splitting, explicit chain/read-write detail). Classification was changed from the caller-supplied `new-feature` to `design-oversight` because the dominant effect of this diff is a loss of previously-earned detail, not a new capability — though the two genuinely new audit flags (orphaned skill, hardcoded-path inconsistency) are a real improvement and worth keeping.
**Lesson**: **[Rule]** When regenerating a diagram or artifact "from scratch," diff the new version against the last committed version (and against the artifact's own lessons-learned history) before finalizing, and never silently reduce granularity or detail that a previous pass deliberately added — a full regeneration should be a superset of prior improvements plus new findings, not a reset to a simpler baseline.
**Commit**: `uncommitted` — documentation

---
