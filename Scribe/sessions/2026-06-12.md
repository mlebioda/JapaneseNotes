## 12:48 — reading-jlpt.md (five targeted fixes)

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Applied five targeted fixes: added tools frontmatter, added mkdir -p directory guard before session file write, removed the contradicting "Does not create JPLessons/Reading/" note, documented the fill-templates/fill_extract.py interface contract, and replaced date-only session filenames with ISO 8601 compact datetime (YYYYMMDDTHHMMSS) to prevent same-day collisions. Also added a no-passage fallback sentence to the Trigger section.
**Why**: Applied Plans/reading-jlpt-fixes-plan.md — five runtime gaps identified after initial skill creation that would only surface during actual use.
**Lesson**: **[Rule]** When implementing a skill from a fixes plan, verify that the session file, lessons-learned entry, and all internal cross-references (e.g. "Does not do X" lists, example filenames in prose) are updated atomically — a fix applied to the procedure but not to the disclaimer or example text leaves the skill internally inconsistent.
**Commit**: uncommitted

---

## 13:48 — instructions.md

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: Added `reading-jlpt` entry to the Available skills table in `.cowork/instructions.md`, registering the new JLPT N4 reading comprehension drill skill with its description, output path, and trigger phrases.
**Why**: Plans/reading-jlpt-plan.md step 2 required registering the skill in instructions.md so it is discoverable by both the orchestrator and the user.
**Lesson**: **[Rule]** When a new skill file is created, register it in instructions.md's Available skills table in the same change set, including the output path and all trigger phrases — a skill that is not registered is invisible to the orchestrator and cannot be invoked by name.
**Commit**: uncommitted

---

## 12:51 — reading-jlpt.md (five targeted fixes, re-capture)

**Called by**: skill-implementer
**Classification**: bug-fix
**What changed**: Same five fixes as 12:48 entry — tools frontmatter, mkdir -p guard, removed contradicting disclaimer, fill_extract.py interface note, ISO 8601 compact datetime filename. Re-captured with caller-provided bug-fix classification.
**Why**: Applied Plans/reading-jlpt-fixes-plan.md — five runtime gaps (missing tools declaration, no directory guard, undocumented chain interface, filename collisions, no no-passage fallback) that would only surface at runtime.
**Lesson**: **[Rule]** When five or more runtime gaps are patched in a single skill revision, classify it as bug-fix rather than improvement — "improvement" understates the severity and makes the change harder to triage in a git-sweep.
**Commit**: uncommitted

---

## 14:05 — vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Vault system diagram regenerated to add reading-jlpt and templates-update skills, correct Caligraphy/ path (was Kaligrafia/), add practice-grammar .ics output, add a new references-skills package (card-templates, verb-conjugation, adj-forms, kanji-links), and tighten all agent notes to match current file content (access permissions, scribe call points, Bash restrictions).
**Why**: Multiple skills and features had been added since the last diagram update and the diagram had drifted out of sync with the actual system structure — including wrong directory names and missing nodes.
**Lesson**: **[Rule]** A documentation agent must be triggered after every skill or agent change, not only after major milestones — a diagram that accumulates multiple stale entries becomes misleading enough to cause misrouting in A2A design reviews.
**Commit**: uncommitted

---

## 12:37 — reading-jlpt.md (fixes plan)

**Called by**: planner
**Classification**: bug-fix
**What changed**: New plan created targeting five runtime-gap fixes to the reading-jlpt skill: missing tools frontmatter, no directory guard for JPLessons/Reading/, undocumented fill-templates interface contract, filename collision from date-only session names, and no fallback when the skill is triggered without a passage.
**Why**: The reading-jlpt skill was functional but had multiple gaps that would only surface at runtime — missing tools declaration, no mkdir guard, ambiguous session filenames, and no instruction for the no-passage invocation case.
**Lesson**: **[Rule]** Before marking a new skill as done, audit it against a standard gap checklist: tools frontmatter present, output directories guarded with mkdir -p, interface contracts with chained skills documented, filenames include a time component to avoid collisions, and all trigger variants (including empty invocation) have explicit fallback instructions.
**Commit**: uncommitted

---

## 12:59 — Plans/reading-jlpt-plan.md, Plans/reading-jlpt-tasks.md

**Called by**: planner
**Classification**: bug-fix
**What changed**: Four implementation issues corrected in the reading-jlpt plan: session filename format changed to YYYYMMDDTHHMMSS, mkdir -p replaced with a fail-loud directory existence check (stop with error if missing), Read removed from skill frontmatter tools list, and Step 7 fill-templates chain wording made explicit ("load .cowork/skills/fill-templates.md and follow its instructions").
**Why**: The plan contained instructions that would produce runtime errors or silent misbehaviour: date-only filenames collide within a day, mkdir -p hides iCloud sync problems, listing Read in tools allows the skill to read files it has no business touching, and "chain to fill-templates" was ambiguous about the exact invocation mechanism.
**Lesson**: **[Rule]** When a skill must not create a directory (e.g. because absence signals an iCloud sync failure), replace any mkdir -p guard with a fail-loud existence check that stops and surfaces the error — silent directory creation masks infrastructure problems that the user needs to know about.
**Commit**: uncommitted

---
