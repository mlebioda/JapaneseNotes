## 11:14 — fix-kanji-headers-plan.md

**Called by**: planner
**Classification**: new-feature
**What changed**: New plan created to make the kanji-headers skill non-destructive by replacing its from-scratch block-write step with an in-place header-format and wikilink correction pass that never touches content below `# Summary`.
**Why**: The current kanji-headers skill replaces everything from `# Summary` onward when a Summary line already exists, silently wiping any kanji block content (reading lines, `## Parts` sections, notes) that the user has built up. The fix replaces that destructive step with a targeted in-place correction that only fixes `##` header formatting and wikilinks.
**Lesson**: **[Rule]** When a skill's job is to format or annotate existing content, implement it as an in-place correction pass — never replace whole sections from a boundary line onward, because that silently destroys user content built up since the last git snapshot.
**Commit**: uncommitted

---

## 14:00 — fix-kanji-headers-plan.md, fix-kanji-headers-tasks.md

**Called by**: planner
**Classification**: improvement
**What changed**: Plan significantly revised to introduce a new `kanji-file` standalone skill and rewrite `kanji-headers` to delegate all per-kanji-file work (web fetch, mnemonic, parts, link verification, bare link migration) to it; `update-kanji-list` receives a deprecation notice rather than being deleted.
**Why**: The original plan addressed only the destructive header-rewrite problem; the revised architecture cleanly separates lesson-file concerns (kanji-headers) from kanji-reference-file concerns (kanji-file), makes each skill independently invocable, guards against infinite recursion via cycle detection, and preserves the deprecated skill file for rollback safety.
**Lesson**: **[Rule]** When deprecating a skill whose logic is being absorbed by two new skills, insert a deprecation notice at the top of the old file and retain it — never delete it in the same change set that introduces the replacements, so a rollback path exists until the new skills are confirmed stable.
**Commit**: uncommitted

---

## 14:22 — kanji-headers.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Rewrote kanji-headers with a 5-step workflow: read headers, ensure kanji reference files exist (delegating to kanji-file per character), fix header formats in-place, write verified wikilinks, update KanjiList.md; removed the old destructive # Summary replacement rule and the image-extraction section.
**Why**: The old skill replaced everything from # Summary onward when that line existed, silently destroying user-built content; image-extraction logic was unrelated to the skill's core job; per-kanji-file work (mnemonics, parts, link verification) now belongs exclusively to the kanji-file skill.
**Lesson**: **[Rule]** When a skill is split into a primary skill and a delegated skill, the primary skill's workflow must explicitly state which steps it performs itself and which it delegates — an ambiguous boundary causes implementers to duplicate logic or skip responsibilities.
**Commit**: uncommitted

---

## 12:24 — kanji-file.md

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: New standalone skill created for processing a single kanji reference file — fetches mnemonic phrase and component explanation from kanji-trainer.org, writes or updates ### Mnemonic and ### Parts sections, verifies all non-lesson wikilinks against actual filenames, migrates bare links to ## Occurences, and runs a consistency check.
**Why**: Splitting per-kanji-reference-file concerns out of kanji-headers into a dedicated skill makes each independently invocable (directly by the user via "kanji-file [character]" or automatically by kanji-headers), prevents the primary skill from duplicating per-file logic, and enables cycle detection to guard against infinite recursion.
**Lesson**: **[Rule]** When a new skill is designed to be callable both directly by the user and programmatically by another skill, its invocation triggers and its A2A interface must both be documented in the skill file itself — without this, callers cannot determine the correct invocation form and user discovery fails.
**Commit**: uncommitted

---

## 14:26 — update-kanji-list.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Deprecation notice added at the top of update-kanji-list.md marking it as superseded by kanji-headers (Steps 2 & 5) and the new kanji-file skill; file retained for reference.
**Why**: The skill's logic has been absorbed into kanji-headers and kanji-file; deleting it in the same change set that introduces the replacements would remove the rollback path before the new skills are confirmed stable.
**Lesson**: **[Rule]** When a skill is superseded, mark it deprecated in-file rather than deleting it — the deletion should be a separate commit after the replacement skills are confirmed stable in production.
**Commit**: uncommitted

---

## 12:27 — .claude/commands/kanji-file.md

**Called by**: skill-implementer
**Classification**: new-feature
**What changed**: Slash command stub created at `.claude/commands/kanji-file.md` that loads `.cowork/skills/kanji-file.md` and passes `$ARGUMENTS`, enabling the user to invoke "kanji-file [character]" directly from the Claude command palette.
**Why**: A skill that is invocable by the user needs a corresponding slash command stub so it appears in the command palette — without the stub the user must manually paste the skill-load instruction every time.
**Lesson**: **[Rule]** When a skill is designed to be directly user-invocable (not just called by other skills), create a matching `.claude/commands/<skill-name>.md` stub in the same change set — a skill without its command stub cannot be discovered or triggered from the command palette.
**Commit**: uncommitted

---

## 14:29 — .cowork/instructions.md

**Called by**: skill-implementer
**Classification**: improvement
**What changed**: Available skills table updated with a new `kanji-file` entry and a `update-kanji-list` DEPRECATED notice, making the new skill discoverable by name and signalling that the old skill is superseded.
**Why**: The kanji-file skill was created and the update-kanji-list skill was deprecated in this session; without matching entries in instructions.md, users and the orchestrator cannot discover the new skill or know that the old one should no longer be used.
**Lesson**: **[Rule]** When a skill is deprecated (not deleted), add a DEPRECATED notice to its instructions.md entry in the same change set — a stale entry with no deprecation marker will continue to be invoked by users and agents that rely on the table for discovery.
**Commit**: uncommitted

---

## 12:34 — docs/vault-system-diagram.puml

**Called by**: documentation
**Classification**: new-feature
**What changed**: Vault system diagram regenerated to reflect the current agent and skill set: kanji-file added as a new component, kanji-headers chain corrected to delegate to kanji-file per-kanji, update-kanji-list marked DEPRECATED, reading-jlpt note tightened, update-grammar chain fully documented, and minor clarifications across commit, scribe, and extract-grammar notes.
**Why**: Diagram regenerated after multiple skill changes in this session (kanji-headers rewrite, kanji-file addition, update-kanji-list deprecation) that left the diagram stale and the documented call-chain for kanji-headers incorrect.
**Lesson**: **[Rule]** When a documentation agent regenerates a diagram, it must explicitly verify and correct all inter-skill chain annotations (not only component existence) — a diagram that lists a new skill but retains the old, incorrect call-chain annotation is more misleading than one with a missing node.
**Commit**: uncommitted

---
