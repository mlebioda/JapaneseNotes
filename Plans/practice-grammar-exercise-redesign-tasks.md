# Practice Grammar — Exercise Type Redesign — Tasks

- [x] In `.cowork/skills/practice-grammar.md`, locate the `## Exercise generation` section (lines ~108–128) and remove the four old exercise-type bullets entirely (Translate to Japanese / Fill the blank / Choose the correct form / Build from pieces) — no migration. Replace with the six approved types (Type 1 through Type 6) as specified in the plan Reference section.
- [x] In the same `## Exercise generation` section, replace the "Variety" bullet with the deterministic selection rule (single word/expression → Type 1 or 6; group of similar forms → Type 2 or 4 with confusability gate; counters/classifiers → Type 3; context-dependent → Type 5; already-seen point → prefer Type 6). Add the variety sub-rule: vary types only when multiple types are equally valid for a given grammar point; never override the pedagogically correct type just for variety.
- [x] Add an explicit hidden-target rule to `## Exercise generation`: Claude holds target grammar point name internally; exercise title must show only `Exercise N / T` — the exercise title shows only the number, never the grammar point name. Grammar point name may appear in grading feedback after submission; it must never appear in the exercise prompt.
- [x] Update Gate 1 wording to explicitly reference grammar-name suppression: "Prompt does not leak the answer — grammar point name must not appear anywhere in the exercise shown to the student before they answer." Remove any redundant wording that the updated gate now covers.
- [x] Update the batch-mode layout example (lines ~148–162) to remove `— grammar point: …` and `[use case: …]` labels from the exercise header line shown to the user. The only visible header is `Exercise N / T`.
- [x] Update the explanatory note below the batch-mode layout example to state that both the grammar-point name and the use-case label are suppressed from the exercise prompt — Claude holds them internally only.
- [x] In the `## Grading` section, add after the "Weak-point strings" rule: feedback must name the specific semantic or grammatical mismatch in the format `"you used X (meaning/use) but the situation requires Y (meaning/use)"` with a concrete example. Note that grammar point name is permitted in grading feedback after submission.
- [x] In the `## Grading` section, add type-specific grading rules for Type 5 and Type 6 (see plan Step 6 for exact wording).
- [x] In `## Exercise generation` or a dedicated note, add the weak-point bias rule: for weak-point reinforcement (low SM-2 ease or recent failure), prefer Type 1 or Type 3; avoid Types 5 and 6 when targeting known weak conjugations.
- [x] Add an explicit session summary exemption note near the hidden-target rule: grammar point headers are permitted in the post-session summary (session is over; exposure is appropriate for review).
- [x] Add the confusability definition for Type 2 in the type description: all four choices must be grammatically plausible in the given sentence; the lesson must contain at least two forms that share a morphological relationship with the target (e.g. all conditionals, all て-forms, all aspect pairs).
- [x] Verify the three existing non-trivial exercise gates (Gate 1 / Gate 2 / Gate 3) remain intact and are not accidentally removed during the edit. Gate 1 should now reference the grammar-name suppression rule explicitly.
- [x] After completing all edits, search the skill file for any occurrence of the old type names ("Translate to Japanese", "Fill the blank", "Choose the correct form", "Build from pieces") and confirm none remain anywhere in the file.
- [x] Read the final edited skill file in full and confirm: (a) no `<!--ID:-->` lines or TARGET DECK line exists in this file (expected — it is a skill, not a lesson), (b) all six type descriptions are present with grading notes for Types 5 and 6, (c) the batch-mode example no longer shows the grammar-point header to the user, (d) the hidden-target rule explicitly covers the grammar point name, (e) Gate 1 wording references the grammar-name suppression rule.
