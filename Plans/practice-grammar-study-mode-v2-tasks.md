# Practice Grammar — Study Mode v2 — Tasks

- [x] Edit `## Workflow` step 7e in `.cowork/skills/practice-grammar.md`: replace "Ready for next batch? (yes / stop)" with the two-prompt spec — non-last batch: "Next batch / Study mode"; last batch: "Study mode / Save & finish". Preserve standalone "stop"/"end" as an implicit early-exit escape at any point.
- [x] Insert new `## Study Mode` section in `.cowork/skills/practice-grammar.md` immediately after `## Interaction flow`: trigger, topic selection (Step 1 — "none" skips to Step 3 directly), per-topic loop (Steps 2a–2d: explanation with user answer reference and answer-retrieval fallback, Practice/Next topic/Questions prompt, branch logic, best-effort exercises with min 4 valid / skip-and-report rule / local study_counter / session counter freeze, post-practice Next topic/Questions prompt), and post-study prompt (Step 3: "Next batch / Finish" or direct post-session if last batch).
- [x] Edit `## Interaction flow` in `.cowork/skills/practice-grammar.md`: add one sentence — "Study Mode exercises use a local `study_counter` and are excluded from the session-wide progress indicator."
- [ ] Edit `## Persistence` in `.cowork/skills/practice-grammar.md`: add sentence "Study Mode exercises do not contribute to `score_buffer` or `weak_point_buffer`. Only main batch exercises produce scores that are written to `grammar-state.json`."
