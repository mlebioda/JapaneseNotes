# Agent System — Precision Rule — Tasks

- [x] In `.claude/agents/planner.md`, in the `### Conversation flow` section under **Step 1 (Understand the goal)**, add a **Precision gate** paragraph: before writing any plan, check whether the request contains underspecified details (vague verbs: "make exercises", "generate questions", "study mode", "review", "practice" without a defined format); if so, ask one focused clarifying question that pins it down; do not write the plan until the ambiguity is resolved. Include a concrete example question.
- [x] In `.claude/agents/skill-implementer.md`, in `### Step 3 — Self-review`, add a **precision check** item to the per-file checklist: for each step in the skill, ask "if I follow this literally, do I know exactly what to produce?"; rewrite any step that fails (vague counts, unnamed types, "as applicable", "where relevant", "if needed" without a defined condition) before marking the task complete; include explicit named checks: counts explicit, types named, branching conditions stated precisely.
- [x] In `.claude/agents/reviewer.md`, in **Step 2b — Missing rules / edge cases**, add a **Predictability** standing check item: for each step in the skill's workflow, check whether a reader can predict the exact output without relying on model judgment; flag each step that uses "generate appropriate", "explain as needed", "add relevant examples", or equivalent soft language as a moderate issue with the problematic phrase quoted.
- [x] In `.claude/agents/reviewer.md`, in **Step 2f — Proactive suggestions**, add a **Precision [standing check]** item: scan for soft language that survived 2b ("as appropriate", "where relevant", "a few", "some", "several", "if needed" without defined condition, "naturally"); flag each as [optional] precision improvement only where a more concrete rule is feasible.
- [x] After all three agent files are updated, read each one back and verify: (a) the precision additions are present and unambiguous, (b) no existing rules were removed or shifted, (c) YAML frontmatter is intact, (d) the new text itself passes the precision test — no vague language in the precision rule wording.
