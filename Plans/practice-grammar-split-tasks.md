# Practice Grammar Skill Split — Tasks

- [x] **Step 1a** — Edit `.cowork/skills/practice-grammar.md`: remove `## Study Mode` section (lines 358–492 in current file); replace with load-directive block pointing to `practice-grammar-study-mode.md` with context fields: `batch_topics`, `user_answers`, `batch_number`, `num_batches`, `is_last_batch`, `vocab_pool`, `weak_points`
- [x] **Step 1b** — Edit `.cowork/skills/practice-grammar.md`: remove `## Persistence` section (lines 547–649) and `## Calendar sync` section (lines 651–682); replace with load-directive block pointing to `practice-grammar-srs.md` with context fields: `session_scores`, `session_grammar_ids`, `today`, `scope`
- [x] **Step 1c** — Edit `.cowork/skills/practice-grammar.md`: add prominent BETWEEN-BATCH GATE callout block (marked MANDATORY) immediately before the bullet points in Workflow step 7e; also update the early-exit "stop" bullet in step 7e to explicitly say "load `practice-grammar-srs.md`" instead of "write grammar-state.json"
- [x] **Step 1d** — Verify main skill after edit: sections Frontmatter, Trigger, Scope resolution, Workflow, Parsing, Exercise generation, Interaction flow, Grading, Session summary, Never touch are all intact; line count is in the 420–460 range
- [x] **Step 2** — Create `.cowork/skills/practice-grammar-study-mode.md`: write YAML frontmatter + context-required header + full Study Mode section (Steps 1–3) extracted verbatim from the current skill; confirm "Do NOT write to grammar-state.json" rule is present
- [x] **Step 3** — Create `.cowork/skills/practice-grammar-srs.md`: write YAML frontmatter + context-required header + full Persistence section (SM-2 table, load control) + full Calendar sync section extracted verbatim from the current skill; confirm `ics-export.py` shell command is present
- [x] **Step 4** — Smoke-check: confirm `## Study Mode` heading still exists in main skill (load-directive version), `## Grading` and `## Session summary` sections are undisturbed, between-batch gate callout contains the word "MANDATORY", sub-skills contain their respective verbatim content
